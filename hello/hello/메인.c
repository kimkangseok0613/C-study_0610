#include <stdio.h>
#include "lectures.h"

/*
	전처리기 : "#" +"키워드"
	기능		: 컴파일 전에 전처리기에 등록된 기능을 수행한다.
	예시		: #include + 파일 이름

	#include <파일 이름> : 시스템 경로에서 해당 파일이 있는지 없는지 확인합니다.
	#include "파일 이름" : 사용자가 만든 헤더 파일을 먼저 검사하고, 만약에 없으면 시스템 경로에서 찾습니다.
*/
int main(void)
{
	//lecture1();
	//lecture2();
	//lecture3();
	//lecture4();
	//lecture5();
	//lecture6();
	//lecture9();
	//lecture10();
	lecture11();

	return 0;
}